[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn ghl.webhook_handler:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
